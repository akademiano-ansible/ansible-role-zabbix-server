---

- name: Install timescaledb
  include_role:
    name: egeneralov.timescaledb
  vars:
    pgdg_users: "{{ egeneralov.timescaledb.pgdg_users }}"
    pgdg_postgresql_conf: "{{ egeneralov.timescaledb.pgdg_postgresql_conf }}"
